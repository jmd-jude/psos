/* AI Insights API Handler (Claude Integration) */

import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'AI Insights API - Coming soon' });
}
